pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Git-checkout') {
      steps {
        sh 'echo passed'
      }
    }
    stage('maven build') {
      steps {
        sh 'mvn --version'
        echo 'maven build works'
      }
    }
  }
}
