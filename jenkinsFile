pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('git-checkout') {
      steps {
        sh 'echo passed'
      }
    }
    stage('maven build') {
      steps {
        sh 'clean install package'
        echo 'maven build works!'
      }
    }
  }
}
