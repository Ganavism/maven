pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('git-checkout') {
      steps {
        sh 'echo passed'
      }
    }
    stage('maven build') {
      steps {
        sh 'mvn clean package'
        echo 'maven build works!'
      }
    }
  }
}
